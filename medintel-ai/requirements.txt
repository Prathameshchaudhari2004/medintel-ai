# MedIntel AI — Project Dependencies
# Install with: pip install -r requirements.txt
#
# Pinning to stable ranges instead of exact versions
# so it works across Python 3.9 - 3.12

# ── Core App ──────────────────────────────────────
streamlit>=1.32.0

# ── Data & ML ─────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# ── AI / LLM — OpenRouter API (free, works in India) ─
# No package needed — uses Python built-in urllib
# Get free key from: openrouter.ai

# ── PDF Processing ────────────────────────────────
pdfplumber>=0.10.0
PyPDF2>=3.0.0
reportlab>=4.0.0         # for PDF report generation

# ── Visualization ─────────────────────────────────
plotly>=5.18.0

# ── Translation ───────────────────────────────────
deep-translator>=1.11.0

# ── Database ──────────────────────────────────────
# SQLite is built into Python — no extra package needed

# ── Optional: OCR for scanned PDFs ───────────────
# Uncomment if you need OCR support:
# easyocr>=1.7.0
# PyMuPDF>=1.23.0       # for converting PDF pages to images for OCR
